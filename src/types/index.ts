export interface Project {
  title: string;
  description: string;
  tags: string[];
  url?: string;
}

export interface Studies {
  title: string;
  description: string;
  tags: string[];
  url?: string;
}

export interface Social {
  name: string;
  url: string;
  icon: string;
  description: string;
}
